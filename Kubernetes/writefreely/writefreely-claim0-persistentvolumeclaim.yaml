apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: writefreely-claim0
  name: writefreely-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
